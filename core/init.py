"""
core package

Marks `core/` as a Python package so modules can be imported as
`core.config`, `core.state`, `core.logger`, `core.router`,
`core.orchestrator`.

Deliberately empty: no side effects should happen on package import
(MAFS Ch.8 — no hidden behavior, everything explicit).
"""
